def list_shift(lista,float):
    for i in range(len(lista)):
        lista[i] += float

def calc_avg(lista):
    total = sum(lista)
    cantidad = len(lista)
    return total / cantidad
def print_normalizad(lista):
    print(lista)
    
   